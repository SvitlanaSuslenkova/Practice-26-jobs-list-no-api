import { useDispatch } from "react-redux";
import { setRoleFilter } from "../redux/slices/filterSlice";

function Filter() {
  const dispatch = useDispatch();
  return <div>Filter</div>;
}

export default Filter;
